# Local database connection settings.
# These defaults match a local SQL Server instance set up per script.sql,
# so this works out of the box on a fresh machine without editing anything.
#
# To point at a different host/database/login, just change the values below.
db.host=localhost
db.port=1433
db.name=Cinema_Booking_System
db.user=java_user
db.password=java123
